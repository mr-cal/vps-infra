[Journal]
# Cap the persistent journal so runaway logging can't fill the disk on this
# small (24GB) VPS. Root-caused the 2026-07-23 disk-full incident that led
# to Remark42 comment data loss — /var/log/journal had grown to 911MB with
# no limit configured.
SystemMaxUse=200M
